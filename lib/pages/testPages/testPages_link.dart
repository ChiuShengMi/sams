import 'package:flutter/material.dart';

class TestPageLink extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Test Page Link'),
      ),
      body: Center(
        child: Text('This is the TestPageLink.'),
      ),
    );
  }
}
